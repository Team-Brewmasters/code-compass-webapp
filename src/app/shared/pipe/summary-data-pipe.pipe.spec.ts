import { SummaryDataPipePipe } from './summary-data-pipe.pipe';

describe('SummaryDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
